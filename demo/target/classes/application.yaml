spring:
  datasource:
    url: jdbc:mysql://localhost:3306/strettogelato
    username: ${db_usuario}
    password: ${db_contraseña}
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.MySQLDialect